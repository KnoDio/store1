<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lin.dao.ResponMapper">

    <insert id="add" parameterType="com.lin.pojo.Respon">
        insert into ssmbuild.respontable(uid, resmessage)
        values (#{uid},#{resmessage})
    </insert>

</mapper>